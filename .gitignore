# Python virtual environment (created with `uv venv`)
.venv/

# MkDocs build output (`mkdocs build` / `mkdocs gh-deploy` generate this)
site/

# Python caches
__pycache__/
*.py[cod]

# Local Hermes config (distributed via Drive link, not served from the site)
hermes-config.json
